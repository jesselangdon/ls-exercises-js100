// Remove the first element of the array, and an element as the array's end.

let energy = ['fossil', 'solar', 'wind', 'tidal', 'fusion'];
energy.shift('fossil');
energy.push('geothermal');
console.log(energy)