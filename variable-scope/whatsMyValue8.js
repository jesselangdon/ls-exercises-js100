let a = 1;

function myFunction(a) {
  console.log(a);
}

let b = 2;

myFunction(b);

// should log 2

// Because b is assigned to 2, the value 2 is supplied to myFunction as an argument,
// which is then used as a parameter within the function.